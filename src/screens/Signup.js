import React from "react";
import { TextInput, Text } from "react-native-paper";
import { StyleSheet, View } from "react-native";

export default function Signup({ style, ...props }) {
  return (
    <View>
      <Text>Signup</Text>
    </View>
  );
}
