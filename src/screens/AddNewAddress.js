import React from "react";
import { TextInput, Text } from "react-native-paper";
import { StyleSheet, View } from "react-native";

export default function AddNewAddress({ style, ...props }) {
  return (
    <View>
      <Text>Add New Address</Text>
    </View>
  );
}
