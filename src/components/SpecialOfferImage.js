import React from "react";
import { TextInput, Text } from "react-native-paper";
import { StyleSheet, View } from "react-native";

export default function SpecialOfferImage({ style, ...props }) {
  return (
    <View>
      <Text>Special offer image</Text>
    </View>
  );
}
